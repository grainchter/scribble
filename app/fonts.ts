import { Montserrat } from "next/font/google";

export const montseratt = Montserrat({
  weight: ["100", "200", "300"],
  display: "swap",
  subsets: ["latin", "cyrillic"],
  variable: "--font-inter",
});
